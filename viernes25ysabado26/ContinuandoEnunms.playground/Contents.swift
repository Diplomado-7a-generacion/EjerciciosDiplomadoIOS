import UIKit
/*
enum ContactMethod{
    case email(String)
    case phone(Int)
    case mail(streetName: String, zipCode: String, streetNumber: Int)
}
var myPreferredContactMethod = ContactMethod.email("myemail@email.me")
switch myPreferredContactMethod {
case .email(let strEmail): print(strEmail)
case .phone(let num): print(num)
case .mail(streetName: let num):
    let zc, streetNumber: let num):
    print("Address street \(name) \(num),\(zc)")
}
enum ArithmeticExpression{
    case number(Int)
    indirect case
        addition(ArithmeticExpression,ArithmeticExpression)
    indirect case
        multiplication(ArithmeticExpression, ArithmeticExpression)
    indirect case
        division(ArithmeticExpression, ArithmeticExpression)
}
//((5+4)*2) en cada case vemos que se hace si suma o multiplicacion
let five = ArithmeticExpression.number(5)
let four = ArithmeticExpression.number(4)
let six = ArithmeticExpression.number(6)
let eigth = ArithmeticExpression.number(8)
let one = ArithmeticExpression.number(1)
let sum = ArithmeticExpression.addition(five, four)
let product = ArithmeticExpression.multiplication(four,resta)
let resta = ArithmeticExpression.rest(eigth,one)

func evaluate(expression:
              ArithmeticExpression) -> int{
    switch expression{
    case .number(let number ): return number
    case .addtion(let leftExp, let rightExp):
    case .muliplication(let leftExp, let rightExp): return
        evaluate(expression: leftExp) * evaluate(expression: rightExp)
    }
}
// 17 o 20
// (6+(4*(8-1))/2)
evaluate(expression: product)
*/

/*
class Car {
    var wheels: Int
    var color : String
    
    init(wheels: Int, color: String){
        self.wheels = wheels
        self.color = color
    }
}
let myCar = Car(wheels:4, color: "Black")
myCar.color = "Blue"
let copyOfMyCar = myCar
copyOfMyCar.color = "Red"

struct Dog{
    var name : String
    var isAdopted: Bool
}
var dante = Dog(name : "Dante", isAdopted: true)
dante.name = "Juan"
var danteClone = dante
danteclone.isAdopted = false
print(dante.isAdopted)
print(danteClone.isAdopted)

*/


